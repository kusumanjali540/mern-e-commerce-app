import React from "react";

const Logo = ({ className }) => {
  return (
    <div className={`h-full ${className}`}>
      <img src="/img/Tech Gadget KV.png" alt="logo" className="h-full"/>
    </div>
  );
};

export default Logo;
